import org.olf.numgen.NumberGeneratorSequence

import groovy.transform.*

@Field
NumberGeneratorSequence numberGeneratorSequence

json g.render (numberGeneratorSequence, [expand:[], excludes: []]) {
}

